# Public helpers

The current public catalogue has no macros. Full descendant and direct text are
columns of `html_element`; use ordinary SQL for filtering and truncation.
`HELPERS` remains the explicit registry consumed by catalogue installation and
`GET /query/helpers`. Future helpers require an active use case, a complete SQL
contract, and correctness/resource tests.
