import type { Metadata } from "next"
import Link from "next/link"
import { ArrowUpRight } from "lucide-react"
import { HtmlStructureIllustration } from "@/components/illustrations/html-structure"
import { TextPlacementIllustration } from "@/components/illustrations/text-placement"
import { DatasetSql } from "@/components/dataset-preview"
import { featuredDataset } from "@/lib/datasets"
import { Table, TableHeader, TableHead, TableBody, TableRow, TableCell } from "@/components/ui/table"

export const metadata: Metadata = {
  title: "The idea behind Periplus",
  description: "The idea behind Periplus: the web as one dataset, a shared tabular structure, and meaning defined by the people querying it. Explore the model, a worked example, and current coverage limits.",
}

export default function AboutPage() {
  return <main className="about-page">
    <header className="about-hero"><span className="eyebrow">About / The idea behind Periplus</span><h1>One web.<br /><span>Many ways to see it.</span></h1><p>Periplus starts with an idea: treat the web as one dataset. Represent its pages through a shared tabular structure, then let people decide what to look for, how to connect it, and what it means.</p><nav className="about-contents" aria-label="On this page"><a href="#vision">The perspective</a><a href="#recipe">Worked example</a><a href="#structure">Data model</a><a href="#how-it-works">Coverage & time</a><a href="#agents">Agents</a><a href="#access">Access & data use</a></nav></header>

    <section id="vision" className="about-section"><span className="eyebrow">01 / The perspective</span><div><h2>A different view<br />of the same web.</h2><p>A browser gives you a page to read. Periplus gives you structure to reason across: text, elements, attributes, and links, expressed through one shared data model. You can filter a set of pages, compare their contents, or follow relationships through SQL.</p><p>Imagine the web as a spreadsheet whose columns you can define for each question. A heading can become a book title in one analysis, a topic in another, or simply an HTML element. The underlying representation keeps that choice open.</p><h3>Structure first. Interpretation yours.</h3><p>Periplus represents the structure of HTML without requiring pages to fit predefined categories such as products, companies, or articles. The model preserves elements and their relationships; your query supplies the analytical meaning. A new question can reveal a different dataset in the same material.</p><p>Our vision is to make this perspective available across the web. Today’s preview covers a small corpus, using the same shared model. <Link href="/coverage">Live coverage</Link> shows what is represented now, and <Link href="/datasets">runnable datasets</Link> show what you can do with it.</p><h3>A shared foundation that grows in usefulness.</h3><p>The value comes from rich structure, useful coverage, and analyses people can build on. A new source can serve many questions; a new query can uncover value in pages already here. Periplus maintains that foundation, while users help shape its reach by suggesting sources and sharing the questions they want to answer.</p><p>Over time, focused needs can help fund broader and fresher coverage. The preview accepts coverage requests with a starting URL or a description of the data needed; scheduled inclusion and freshness guarantees are future possibilities.</p></div></section>

    <section id="recipe" className="about-section"><span className="eyebrow">02 / A worked extraction</span><div><h2>From book pages<br />to a price comparison.</h2><p>A book title and a price appear as <code>h1</code> and <code>p.price_color</code> elements. In Periplus, you can query those elements as rows in the same table. This query pairs a title with a price under the same parent, keeping recommendation prices elsewhere on the page out of that pairing.</p><ol className="collection-steps"><li><strong>Choose the pages and their observation.</strong><p>Filter to Books to Scrape product URLs. Keep one observation per requested URL using the latest recorded date, with an ID tie-breaker. Select at most 20 URLs before joining their elements. Undated observations sort last; their actual collection order is unknown.</p></li><li><strong>Define your fields from structure.</strong><p>Join on <code>content_id</code> to reach the HTML. Match a heading and a price with the same <code>parent_index</code>, filter the price by its class attribute, and parse the amount into a decimal column.</p></li><li><strong>Keep the context with the data.</strong><p>Return the source URL and collection date beside the title and price. The source is a practice website: this demonstrates product extraction, not a live market dataset.</p></li></ol><DatasetSql sql={featuredDataset.sql} /><div className="about-actions"><Link className="story-link" href="/datasets/books-with-prices">Run the dataset and inspect its rows <ArrowUpRight /></Link></div><h3>Adapt the question, then check the scope.</h3><p>Change the selected fields or filters to suit your analysis. Expanding the URL sample changes which pages are eligible; increasing the final LIMIT only changes how many matching rows can be returned. An output limit does not limit the number of input rows scanned.</p><p>Structure gives you extraction control, not a universal product schema. Site-specific rules can need changes when page layouts change. Nested text also matters: <code>text_direct</code> excludes child-element text, as the example below explains.</p></div></section>

    <section id="structure" className="about-section"><span className="eyebrow">03 / The data model</span><div><h2>Know what a row means.</h2><p>The shared model connects page observations, content objects, HTML elements, and links. These four related tables describe the source structure. Business entities such as companies, products, or articles are views you define through SQL.</p><div className="about-table"><Table><TableHeader><TableRow><TableHead>Relation</TableHead><TableHead>One row represents</TableHead></TableRow></TableHeader><TableBody>{[["web.observation", "A URL observation, including unsuccessful collection. Repeated observations are separate rows."], ["content.object", "A unique retained byte sequence, identified by content_id."], ["content.html_element", "An HTML element, identified by content_id and element_index."], ["web.link_occurrence", "An anchor occurrence in a particular observation; repeated links remain separate."]].map(([name, description]) => <TableRow key={name}><TableCell><Link href={`/discover?${new URLSearchParams({ mode: "sql", sql: `DESCRIBE ${name};` })}`}><code>{name}</code></Link></TableCell><TableCell>{description}</TableCell></TableRow>)}</TableBody></Table></div><h3>Choose the grain before joining.</h3><p>Use <code>content_id</code> to join observations to content and HTML elements. Identical captured bytes can appear in many observations, so joining all observations repeats their element rows. Select an observation per URL for a page-level view, or deduplicate content IDs when you want each captured document only once.</p><p>Join links to observations using <code>observation_id</code>. Links carry observation context because a relative address can resolve differently when identical HTML appears at different source URLs. A link target need not be a collected page.</p><HtmlStructureIllustration /><h3>Retain structure; define meaning yourself.</h3><p>Elements preserve tags, namespaces, attributes, direct and trailing text, hierarchy, document order, and subtree boundaries. Parsing normalizes HTML, so the projection is not a byte-for-byte reconstruction. Formats other than HTML can appear in <code>content.object</code> without an HTML projection.</p><TextPlacementIllustration /><p>The original captured content is retained separately. The public preview exposes the structural SQL projection and source metadata; it does not provide a raw-file download or a historical browser replay. A source URL opens the live site, which may have changed since collection.</p></div></section>

    <section id="how-it-works" className="about-section"><span className="eyebrow">04 / Coverage & time</span><div><h2>A view of the web.<br />Grounded in time.</h2><p>Every view has a source and a moment. Behind the scenes, browser-based collection captures a document after configured loading and interaction steps; imports can bring content acquired elsewhere. That document records one observed state of a page, rather than every possible interactive or personalised state.</p><p>Periplus retains captured content immutably and shares identical byte sequences through content identities. It records observations, then builds HTML and link projections separately. Collection can finish before those projections are queryable.</p><p>The preview includes selected sources and imports. It offers neither a representative sample of the web nor a uniform refresh schedule. Collection dates can be missing; the latest date for a site does not establish that all its pages are fresh.</p><p>Coverage requests are saved, then picked up automatically. You can suggest a URL or describe the data you need, with a depth, link scope, and page budget. Descriptions are resolved to starting pages using AI and search. Requests initiate one-time collection, without scheduled refreshes. The public request list shows pending, ongoing, and recently completed requests.</p><div className="about-actions"><Link className="story-link" href="/coverage">Inspect coverage <ArrowUpRight /></Link><Link className="story-link" href="/suggest">Suggest coverage <ArrowUpRight /></Link></div></div></section>

    <section id="agents" className="about-section"><span className="eyebrow">05 / Asking with an agent</span><div><h2>A question becomes SQL.<br />SQL produces the rows.</h2><p>Ask mode helps formulate an analysis, queries the public tables, and explains the result. SQL mode lets you work directly. Both explore the same tabular foundation through a read-only query service. A question works with available data; it does not add new pages.</p><p>A browser agent is useful for interacting with live pages. Periplus gives an agent another lens: a structured dataset it can filter, join, and aggregate before rows reach the model. That can reduce repeated navigation and page-by-page interpretation; the benefit depends on the query and coverage.</p><p>Completed analyses show a finding, returned rows or a single value, and a scope note. Expand the SQL to inspect the method or open it in the bench. Query activity keeps exploratory work separate. A request for SQL alone produces an unexecuted draft.</p><p>Inspect the SQL and evidence when evaluating an AI explanation. A query link saves SQL and parameters, not a frozen dataset; exact repeatability requires the same input data, schema, and deterministic semantics.</p></div></section>

    <section id="access" className="about-section"><span className="eyebrow">06 / Using the preview</span><div><h2>Free to explore.<br />Clear about what you get.</h2><h3>Access and limits</h3><p>The public preview is free and requires no account. Queries run read-only on Periplus servers. SQL responses are limited to 1,000 rows and 8 MiB, with a 20-second execution deadline. Busy periods can require a retry.</p><p>Ask mode receives at most 20 rows per query and may deliberately inspect a smaller input sample. Curated datasets also declare their filters and limits. An aggregate may summarise many input rows even when its output is small. CSV exports contain only the returned rows, not an unlimited download.</p><h3>Questions, queries, and privacy</h3><p>Periplus logs executed SQL, parameters, outcomes, and timings to debug and improve the service. Ask mode sends your conversation text and sampled query results to the model provider. SQL mode does not use an AI model.</p><p>Conversations are held in the browser and disappear on reload; this does not mean server query logs disappear. Query links include SQL and parameters, and questions launched from the home page appear in the destination URL. Keep secrets and private information out of these inputs. Submitted collection URLs and collected data are public.</p><h3>Using collected content</h3><p>“Public” describes access to this preview. Periplus has not published a blanket reuse licence for third-party content or formal service terms. Do not interpret the availability of a row or an export as permission to redistribute the underlying content. Contact us about your intended use before depending on the preview for redistribution or production.</p><p>Formal privacy and data-use policies are not published yet. For questions about stored information, source content, or removal requests, contact <a href="mailto:ekku.leivonen@elei.io">ekku.leivonen@elei.io</a>.</p><h3>Help shape what comes next</h3><p>Send an analysis you want to run, a dataset query worth sharing, or a result that needs attention. Include the query reference when reporting a problem so we can investigate the same execution.</p><div className="about-actions"><a className="story-link" href="mailto:ekku.leivonen@elei.io?subject=Periplus%20feedback">Send feedback <ArrowUpRight /></a><Link className="story-link" href="/datasets">Explore datasets <ArrowUpRight /></Link></div></div></section>
  </main>
}
