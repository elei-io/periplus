{
  "name": "periplus-workspace",
  "private": true,
  "workspaces": [
    "packages/periplus-console-core",
    "packages/periplus-terminal-shell",
    "packages/periplus-web-shell",
    "packages/periplus-admin",
    "packages/periplus-public"
  ],
  "scripts": {
    "build:packages": "npm run build --workspace periplus-console-core && npm run build --workspace periplus-web-shell && npm run build --workspace periplus-terminal-shell",
    "check:packages": "npm run check --workspace periplus-console-core && npm run build --workspace periplus-console-core --silent && npm run check --workspace periplus-web-shell && npm run build --workspace periplus-web-shell --silent && npm run check --workspace periplus-terminal-shell",
    "test:packages": "npm run test --workspace periplus-console-core && npm run test --workspace periplus-terminal-shell",
    "periplus": "npm run build --workspace periplus-console-core --silent && npm run build --workspace periplus-terminal-shell --silent && node packages/periplus-terminal-shell/dist/main.js",
    "build:public": "npm run build --workspace periplus-console-core --silent && npm run build --workspace periplus-web-shell --silent && npm run build --workspace periplus-public",
    "check:public": "npm run build --workspace periplus-console-core --silent && npm run build --workspace periplus-web-shell --silent && npm run typecheck --workspace periplus-public && npm run lint --workspace periplus-public && npm run test --workspace periplus-public",
    "build:admin": "npm run build --workspace periplus-console-core --silent && npm run build --workspace periplus-web-shell --silent && npm run build --workspace periplus-admin",
    "check:admin": "npm run typecheck --workspace periplus-admin && npm run lint --workspace periplus-admin && npm run test --workspace periplus-admin"
  },
  "dependencies": {
    "periplus-terminal-shell": "*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "~6.0.0"
  }
}
