{
  "name": "atlas-shells",
  "private": true,
  "workspaces": [
    "packages/atlas-console-core",
    "packages/atlas-terminal-shell",
    "packages/atlas-web-shell"
  ],
  "scripts": {
    "build:console": "npm run build --workspace atlas-console-core && npm run build --workspace atlas-terminal-shell",
    "check:console": "npm run check --workspace atlas-console-core && npm run build --workspace atlas-console-core --silent && npm run check --workspace atlas-terminal-shell",
    "test:console": "npm run test --workspace atlas-console-core && npm run test --workspace atlas-terminal-shell",
    "atlas": "npm run build:console --silent && node packages/atlas-terminal-shell/dist/main.js"
  },
  "dependencies": {
    "atlas-terminal-shell": "*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "~6.0.0"
  }
}
