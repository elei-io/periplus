#!/usr/bin/env bash
set -euo pipefail

repo_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

exec uv run \
  --project "$repo_dir/packages/periplus" \
  --with "warcio==1.7.5" \
  python -u "$repo_dir/packages/periplus/scripts/test_corpus.py" "$@"
