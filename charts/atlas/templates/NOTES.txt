Atlas web service: {{ include "atlas.fullname" . }}-web
Atlas API service: {{ include "atlas.fullname" . }}-api

The setup hook completed before this release was installed or upgraded. Atlas expects:
- a control PostgreSQL database in {{ .Values.secrets.controlDatabase.name }}
- a distinct DuckLake metadata PostgreSQL database in {{ .Values.secrets.ducklakeMetadata.name }}
- NATS credentials in {{ .Values.secrets.nats.name }}
- S3 credentials in {{ .Values.secrets.s3.name }}
