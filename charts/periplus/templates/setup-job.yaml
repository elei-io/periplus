apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "periplus.fullname" . }}-setup
  labels:
    {{- include "periplus.labels" . | nindent 4 }}
    app.kubernetes.io/component: setup
  annotations:
    helm.sh/hook: pre-install,pre-upgrade
    helm.sh/hook-weight: "-10"
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  backoffLimit: {{ .Values.setup.backoffLimit }}
  activeDeadlineSeconds: {{ .Values.setup.activeDeadlineSeconds }}
  template:
    metadata:
      labels:
        {{- include "periplus.componentSelectorLabels" (dict "root" . "component" "setup") | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      restartPolicy: Never
      {{- include "periplus.backendPodSpec" . | nindent 6 }}
      containers:
        - name: setup
          image: {{ include "periplus.backendImage" . | quote }}
          imagePullPolicy: {{ .Values.images.backend.pullPolicy }}
          command: [periplus-setup]
          {{- include "periplus.envFrom" . | nindent 10 }}
          env:
            {{- include "periplus.backendEnv" . | nindent 12 }}
          resources:
            {{- toYaml .Values.setup.resources | nindent 12 }}
