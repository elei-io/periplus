apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "periplus.fullname" . }}-api
  labels:
    {{- include "periplus.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
{{- if not .Values.api.autoscaling.enabled }}
  replicas: {{ .Values.api.replicas }}
{{- end }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      {{- include "periplus.componentSelectorLabels" (dict "root" . "component" "api") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "periplus.componentSelectorLabels" (dict "root" . "component" "api") | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- include "periplus.corePodSpec" . | nindent 6 }}
      containers:
        - name: api
          image: {{ include "periplus.coreImage" . | quote }}
          imagePullPolicy: {{ .Values.images.core.pullPolicy }}
          command: [fastapi, run, src/periplus/entrypoints/api.py, --host, 0.0.0.0, --port, "8000"]
          workingDir: /app/packages/periplus
          ports:
            - name: http
              containerPort: 8000
          {{- include "periplus.envFrom" . | nindent 10 }}
          env:
            - name: PERIPLUS_QUERY_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.apiAccess.name }}
                  key: {{ .Values.secrets.apiAccess.queryTokenKey }}
            - name: PERIPLUS_SERVICE_VERSION
              value: {{ .Values.images.core.tag | quote }}
            {{- include "periplus.coreEnv" . | nindent 12 }}
            {{- include "periplus.duckdbEnv" .Values.api.duckdb | nindent 12 }}
            - name: PERIPLUS_ADMIN_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.apiAccess.name }}
                  key: {{ .Values.secrets.apiAccess.adminTokenKey }}
            - name: PERIPLUS_PUBLIC_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.apiAccess.name }}
                  key: {{ .Values.secrets.apiAccess.publicTokenKey }}
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 10
          resources:
            {{- toYaml .Values.api.resources | nindent 12 }}
