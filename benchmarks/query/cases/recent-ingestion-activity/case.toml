id = "recent-ingestion-activity"
title = "Recent ingestion activity"
use_case = "Inspect the latest outcomes from one imported dataset while checking an ingestion run."
classification = "both"
ordered = true
memory_limit = "512MB"
max_warm_ms = 60000
