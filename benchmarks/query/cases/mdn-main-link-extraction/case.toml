id = "mdn-main-link-extraction"
title = "Extract anchors from observed MDN content"
use_case = "Export anchor evidence from a bounded set of observed MDN content objects."
classification = "optimizer"
ordered = true
scales = [100, 500, 1000]
memory_limit = "512MB"
max_warm_ms = 60000
