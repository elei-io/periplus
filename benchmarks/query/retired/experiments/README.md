# Superseded layout experiments

These scripts reproduce the body-only prose/content-posting/node-posting layout
at commit `466beb9`. Run them from that revision, not against the new positional
registry. Their completed measurements remain in the investigation documents.
They are historical reproductions, not supported runtime code or compatibility paths.
