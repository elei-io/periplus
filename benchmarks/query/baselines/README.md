# Pinned baselines

Store compact JSON reports here only when their DuckLake snapshot is retained and reproducible.
Live, growing-corpus measurements belong under `.artifacts/query-benchmarks/` and should be
summarized in the optimization report that makes the keep/remove decision.
